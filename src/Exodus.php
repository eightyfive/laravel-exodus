<?php
namespace Eyf\Exodus;

class Exodus
{
    public function parse(array $migrations)
    {
        $normalized = [];

        foreach ($migrations as $name => $fields) {
            //
        }

        return $normalized;
    }
}
